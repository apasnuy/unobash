What Works:
1. SSH
	- keychain integration. No need ssh password typing
		Usage: once key added with "add-ssh -K" oprion, it will be stored in OSx keychain
	- 
2. Screen
	- implicitly uses unobash when starts


u